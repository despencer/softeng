a.b = 5;
a[0] = 6;
a.c();
function a()
 {return num < 0 ? this[ num + this.length ] : this[ num ]; }
function b( elems, callback, matches)
 {
   var i = 0,
   length = elems.length;
   for ( ; i < length; i++ )
       {
       callbackInverse = !callback( elems[ i ], i );
       if ( callbackInverse !== callbackExpect )
          matches.push( elems[ i ] );
       }
  for(var k=0; k && y; f(k)) ++a;
  for( ret(6,7); ;) z=y;
}
function c()
{
 var b=[1,2,3];
 var a=[];
}